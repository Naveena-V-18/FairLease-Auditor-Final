{
  "buildCommand": "npm run build",
  "installCommand": "npm install && cd api && pip install --break-system-packages -r requirements.txt",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/api/upload-lease",
      "dest": "/api/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
