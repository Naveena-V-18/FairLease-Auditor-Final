{
  "buildCommand": "npm run build",
  "installCommand": "npm install && cd api && pip install --upgrade pip && pip install -r requirements.txt",
  "framework": "nextjs",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
